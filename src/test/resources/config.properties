# NDC FlightSearch API environment configuration.
# Any value here can be overridden at runtime with -D<key>=<value>, e.g.
#   mvn test -Dapi.baseUri=https://staging.example.com
#
# api.key is deliberately NOT set here — it's a secret. Supply it via
# -Dapi.key=<key> or the NDC_API_KEY environment variable. See README.md.
api.baseUri=https://ndc-supplier-integration.azurewebsites.net
api.path=/api/V2/FlightSearch/Search
api.clientId=NDC-Core
api.invalidKey=invalid-00000000-0000-0000-0000-000000000000-key
api.supplier=flyadeal
api.credentialsSelector=EGY
